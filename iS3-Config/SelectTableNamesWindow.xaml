<Window x:Class="iS3.Config.SelectTableNamesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:iS3.Config"
        mc:Ignorable="d"
        Title="Select table" Height="300" Width="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <!-- Row 1 -->
        <ListBox x:Name="TableNamesLB" Margin="5,5,5,5" SelectionMode="Multiple"
                 ItemContainerStyle="{StaticResource MyListBoxStyle}"/>

        <!-- Row 2 -->
        <Button x:Name="OKBtn" Grid.Row="1" Margin="0,0,10,0"
                Content="OK" VerticalAlignment="Center" HorizontalAlignment="Right" Width="100" Height="20"
                Click="OK_Click"/>

    </Grid>
</Window>
